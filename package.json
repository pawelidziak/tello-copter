{
    "name": "tello-copter",
    "version": "1.0.0",
    "description": "Tello Copter - Part 2",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "open:console": "tsc --outDir dist src/drone-console.ts && cd dist && node drone-console.js",
        "open:state": "tsc --outDir dist src/drone-state.ts && cd dist && node drone-state.js"
    },
    "author": "Pawel Idziak",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^15.0.2"
    },
    "dependencies": {
        "typescript": "^4.2.4"
    }
}
